import { Advent_Pro as AdventPro, Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '600', '700'],
  display: 'swap',
});

export const adventPro = AdventPro({
  subsets: ['latin'],
  weight: ['600', '700'],
  display: 'swap',
});
